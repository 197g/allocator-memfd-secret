## 1.0.1

- Relaxed `allocator-api2` version requirements to be compatible with `0.2`,
  the version defined in `hashbrown` (the standard library by extension) as
  well as `bumpalo` and a few other allocators.
